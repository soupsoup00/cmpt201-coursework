#include <stdio.h>
#include <unistd.h>

int main() {
  char* line = NULL;
  size_t len = 0;
  FILE *stream;
  char input[];
  bool run = 1;

  while(run){
    printf("Please enter text: ");
    input = getline(&line, &len, &stream);
    
  }
#
}
