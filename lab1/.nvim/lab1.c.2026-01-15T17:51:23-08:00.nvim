#include <stdio.h>
#include <unistd.h>
#include <stdbool.h>
#include <stdlib.h>

int main() {
  char *line = NULL;
  size_t len = 0;
  FILE *stream;
  ssize_t input;
  bool run = 1;

  while (run) {
    printf("Please enter text: ");
    input = getline(&line, &len, &stream);
 
    free(line);
  }
#
}
