#define _GNU_SOURCE
#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <sys/wait.h>
#include <unistd.h>

int main() {
  char *line = NULL;
  size_t len = 0;
  ssize_t = input;

  printf("Enter programs to run.\n");
  input = getline(&line, &len, stdin);
  char *str = line;

  if (input != -1) {
    printf(str);
  }

  // parse user input - strtok

  // tokenize for some reason
  // fork into child and thru child run exec(input command)
  // waitpid for child to terminate before looping process
  free(line);
}
